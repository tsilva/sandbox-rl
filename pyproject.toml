[project]
name = "sandbox-rl"
version = "0.1.0"
description = "Reinforcement learning sandbox for Gymnasium environments"
requires-python = ">=3.10"
dependencies = [
    "torch",
    "numpy",
    "gymnasium[box2d]",
]
